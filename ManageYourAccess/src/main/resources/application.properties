# Application name
spring.application.name=ManageYourAccess

# Spring Security default user credentials
spring.security.user.name=Admin
spring.security.user.password=Password

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Datasource settings (update with your actual DB values)
spring.datasource.url=jdbc:mysql://localhost:3306/passwordmanager
spring.datasource.username=root
spring.datasource.password=20252505

# Correct driver class name
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
